name: Release

# SMOKE (temporary): trigger changed workflow_dispatch -> tag-push to test the
# Gitea v1.27.0 dispatch-validation panic pivot. RESTORED to workflow_dispatch after.
on:
  push:
    tags:
      - 'v*-smoke*'

permissions:
  contents: write

jobs:
  release:
    uses: argyle-labs/.github/.github/workflows/plugin-release.yml@main
    secrets: inherit
    with:
      channel: rc
      bump: patch
